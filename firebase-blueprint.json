{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Stores user preferences and Steam integration details.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Auth UID" },
        "displayName": { "type": "string" },
        "email": { "type": "string" },
        "steamId": { "type": "string", "description": "User's Steam ID" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "createdAt"]
    },
    "GameProgress": {
      "title": "Game Progress",
      "description": "Tracks playtime and progress for a specific game.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "gameId": { "type": "string" },
        "gameName": { "type": "string" },
        "playtimeTotal": { "type": "number" },
        "lastPlayed": { "type": "string", "format": "date-time" },
        "notes": { "type": "string" }
      },
      "required": ["uid", "gameId"]
    },
    "Soundtrack": {
      "title": "Soundtrack Recommendation",
      "description": "AI recommended soundtracks for a game.",
      "type": "object",
      "properties": {
        "gameId": { "type": "string" },
        "recommendations": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "title": { "type": "string" },
              "artist": { "type": "string" },
              "reason": { "type": "string" }
            }
          }
        },
        "generatedClipUrl": { "type": "string" }
      }
    },
    "WishlistItem": {
      "title": "Wishlist Item",
      "description": "A game saved to the user's wishlist.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "gameId": { "type": "string" },
        "gameName": { "type": "string" },
        "imageUrl": { "type": "string" },
        "price": { "type": "string" },
        "addedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "gameId", "gameName", "addedAt"]
    }
  },
  "firestore": {
    "/users/{uid}": {
      "schema": "UserProfile",
      "description": "User profile data"
    },
    "/users/{uid}/games/{gameId}": {
      "schema": "GameProgress",
      "description": "Per-game progress for a user"
    },
    "/games/{gameId}/soundtracks": {
      "schema": "Soundtrack",
      "description": "Global soundtrack recommendations for a game"
    },
    "/wishlist/{wishlistId}": {
      "schema": "WishlistItem",
      "description": "Global wishlist collection using userId_gameId as ID"
    }
  }
}
